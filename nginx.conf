server {
    listen 80;
    server_name aden.io perp.aden.io;
    root /usr/share/nginx/html;
    index index.html;

    # Redirect DEX asset routes from aden.io to perp.aden.io with 301 redirect
    location ~ ^/(demo_trading|perp|portfolio|markets|leaderboard|referral) {
        if ($host = aden.io) {
            return 301 https://perp.aden.io$request_uri;
        }
        # For perp.aden.io, serve SPA directly
        try_files /index.html =404;
    }

    # Disable caching for HTML files
    location ~* \.html$ {
        try_files $uri /index.html =404;
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate, proxy-revalidate";
        add_header Pragma "no-cache";
    }

    # Serve Remix client at root (SPA fallback to index.html)
    location / {
        try_files $uri $uri/ /index.html =404;
        # Disable caching for the root index.html
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate, proxy-revalidate";
        add_header Pragma "no-cache";
    }

    # Cache static assets with versioning support
    location ~* \.(css|js|png|jpg|jpeg|gif|svg|ico|webp|woff|woff2|ttf|eot|mp3|mp4)$ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
}